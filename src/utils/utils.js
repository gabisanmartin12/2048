export const getRandomInt = max => Math.floor(Math.random() * Math.floor(max))
export const getQuotientFrom = (dividend, divisor) => Math.floor(dividend / divisor)
export const getRemainderFrom = (dividend, divisor) => dividend % divisor